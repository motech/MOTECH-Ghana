#if(${staff.getSystemId()})
Support: Case reported by ${staff.getGivenName()} ${staff.getFamilyName()} on ${case.dateRaisedOn}
#else
Support: Case reported by unknown staff on ${case.dateRaisedOn}
#end