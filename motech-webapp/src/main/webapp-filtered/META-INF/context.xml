<Context>
<Resource name="jdbc/motech" 
              auth="Container"
              type="javax.sql.DataSource" 
              username="${dbUsername}" 
              password="${dbPassword}"
              driverClassName="${dbDriver}"
              url="${dbUrl}"
              maxActive="10" 
              maxIdle="4"/>
</Context>