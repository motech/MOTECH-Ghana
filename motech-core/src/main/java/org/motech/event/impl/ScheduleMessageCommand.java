package org.motech.event.impl;

import org.motech.event.Command;

public class ScheduleMessageCommand implements Command {

	public void execute() {
		// Schedule notification of service
		// Check if already scheduled, Not add if already scheduled, remove old
		// scheduled messages
	}

}
